#include <iostream>
using namespace std;

int main()
{
    //----------------------------------------------------------------------------
    /*
    //Find Number whether it is Positive or Negative
     int a = 0;
     cout << "Enter Number: ";
     cin >> a;
     if (a >= 0)
     {
         cout << a << " is Positive  integer." << endl;
     }
     else
     {
         cout << a << " is Negative integer." << endl;
     }

    */

    //----------------------------------------------------------------------------

    /*
     // Find Number whether it is Odd or Even
     int n;
     cout << "Enter Number: ";
     cin >> n;
     if (n % 2 == 0)
     {
         cout << n << " is Even number." << endl;
     }
     else
         cout << n << " is Odd Number" << endl;
    */

    //----------------------------------------------------------------------------

    /*
    // find Smallest number in Array
    int Arr[5] = {7, 3, 4, -9, 2};
    int smallest = INT8_MAX;
    for (int i = 0; i < 5; i++)
    {
        if (Arr[i] < smallest)
        {
            smallest = Arr[i];
        }
    }
    cout << "Smallest Value is " << smallest << " in whole Array."<<endl;

    */

    //----------------------------------------------------------------------------

    /*
    //Find Array smallest Element 2nd Method.
     int Array[6] = {3, 2, 5, 8, -7, 9};
     int smallest = INT8_MAX;
     for (int i = 0; i < 6; i++)
     {
         smallest = min(Array[i], smallest);
     }
     cout << "Smallest Value in Array is " << smallest << endl;

     */
    //----------------------------------------------------------------------------

    /*
    //find Maximum number in Array
     int Array[6] = {2, 3, 4, 8, 6, 5};
     int largest = INT8_MIN;
     for (int i = 0; i < 6; i++)
     {
         if (Array[i] > largest)
         {
             largest = Array[i];
         }
     }
     cout << "Largest Value in Array is " << largest << endl;

     */
    //----------------------------------------------------------------------------

    /*

    // Doubling every element of the Array.
    int Ar[4] = {1, 2, 3, 4};
    for (int i = 0; i < 4; i++)
    {
        Ar[i] = Ar[i] * 2;
    }

    for (int i = 0; i < 4; i++)
    {
        cout << Ar[i] << ", ";
    }

    */
    //----------------------------------------------------------------------------

    return 0;
}